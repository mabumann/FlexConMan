@page
@model FlexConMan_Monolith.Pages.ConferenceCenters.IndexModel

@{
    ViewData["Title"] = "Conference Centers";
    var isUser = User.IsInRole("USER");
}

<h1>@ViewData["Title"]</h1>

@if (!isUser)
{
    <p>
        <a asp-page="Create">Create New</a>
    </p>
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConferenceCenter[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConferenceCenter[0].City)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConferenceCenter[0].Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ConferenceCenter) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @if (!isUser)
                {
                    <a asp-page="./Edit" asp-route-id="@item.CcId">Edit</a> @:|
                }
                <a asp-page = "./Details" asp-route-id = "@item.CcId" > Details </a>
                @if (!isUser)
                {
                    @: |
                    <a asp-page = "./Delete" asp-route-id = "@item.CcId" > Delete </a>
                }
            </td>
        </tr>
}
    </tbody>
</table>
